# Примеры работы с кортежами
tpl = (1, 2, 3)
print(tpl[1])
# tpl[0] = 10  # Ошибка! Кортежи неизменяемы

# Распаковка кортежей
x, y, z = tpl
print(x, y, z)

# Кортежи как ключи словаря
point = (3, 4)
points = {point: "точка"}
print(points) 